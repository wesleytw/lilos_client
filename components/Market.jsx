
import { useMoralis } from "react-moralis";

const Market = () =>{
    return (
      <div>
        <div className="flex flex-wrap ">
          {/* {nfts.map((nftItem, id) => (
            <NFTCard
              key={id}
              nftItem={nftItem}
              title={collection?.title}
              listings={listings}
            />
          ))} */}
        </div>
        <div className=" mx-auto bg-[#ad3a3a] mt-0 md:mt-0 h-screen px-10 py-20">
          <h1>Market</h1>
        </div>
      </div>
    );
}
export default Market;
